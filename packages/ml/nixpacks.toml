[phases.setup]
nixPkgs = ["python310"]

[phases.install]
cmds = [
  "python -m ensurepip --upgrade",
  "python -m pip install --no-cache-dir -r requirements.txt"
]

[phases.build]
cmds = []

[start]
cmd = "python start.py"
