/**
 * Admin Layout - Allows theme switching with full support for both light and dark modes
 */
export default function AdminLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
